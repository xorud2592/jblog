<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="post">

	<select id="selectAll" resultType="postVo">
		<![CDATA[ 
	SELECT p.postNo, c.cateNo, p.postTitle, p.postContent
	FROM category c, post p
	WHERE c.cateNo = p.cateNo
	]]>
	</select>

	<select id="selectAllBycateNo" parameterType="int" resultType="postVo">
		<![CDATA[ 
	SELECT postNo, cateNo, postTitle, postContent
	FROM post
	WHERE ${cateNo} = cateNo
	]]>
	</select>


	<select id="selectByPostNo" parameterType="int" resultType="postVo">
		<![CDATA[ 
	SELECT postNo, cateNo, postTitle, postContent
	FROM post
	WHERE ${postNo} = postNo
	]]>
	</select>
	
	<insert id="insert" parameterType="postVo">
		<![CDATA[
	INSERT INTO post (
		postNo, cateNo, postTitle, postContent, regDate)
	VALUES(
		seq_post_no.nextval,
		#{cateNo}, 
		#{postTitle}, 
		#{postContent},
		sysdate
	)
	]]>
	</insert>
</mapper>