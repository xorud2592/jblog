<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="category">

	<select id="selectAll" resultType="categoryVo">
		<![CDATA[ 
	SELECT c.cateNo, c.userNo, c.cateName, c.description, c.regDate
	FROM category c, users u
	WHERE c.userNo = u.userNo
	]]>
	</select>

	<select id="selectAllByBlog" parameterType="blogVo" resultType="categoryVo">
		<![CDATA[ 
	SELECT cateNo, userNo, cateName, description, regDate
	FROM category
	WHERE ${userNo} = userNo
	]]>
	</select>

	<select id="selectByCateNo" parameterType="int" resultType="categoryVo">
		<![CDATA[ 
	SELECT cateNo, userNo, cateName, description, regDate
	FROM category
	WHERE ${cateNo} = cateNo
	]]>
	</select>

	<insert id="insert" parameterType="categoryVo">
		<![CDATA[
	INSERT INTO category (
		cateNo, userNo,   cateName, description, regDate)
	VALUES(
		seq_category_no.nextval,
		#{userNo}, 
		#{cateName}, 
		#{description},
		sysdate
	)
	]]>
	</insert>

	<delete id="delete" parameterType="int">
		<![CDATA[
	DELETE FROM category
	WHERE cateNo = #{cateNo}
	]]>
	</delete>
</mapper>